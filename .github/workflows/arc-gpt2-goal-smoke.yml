name: ARC-GPT2 Latent Goal Smoke

on:
  workflow_dispatch:
  push:
    branches: [arc-gpt2-programs]
    paths:
      - "arcgpt2/goal_version_runtime.py"
      - "arcgpt2/build_goal_version_dataset.py"
      - "arcgpt2/build_goal_version_compact.py"
      - "arcgpt2/goal_protocol.py"
      - "arcgpt2/compare_goal_version.py"
      - "arcgpt2/tests/test_goal_version_runtime.py"
      - "arcgpt2/tests/test_goal_version_compact.py"
      - ".github/workflows/arc-gpt2-goal-smoke.yml"

permissions:
  contents: write

concurrency:
  group: arc-gpt2-goal-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  experiment:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      TOKENIZERS_PARALLELISM: "false"
      HF_HUB_DISABLE_TELEMETRY: "1"
      ARC_GPT2_GOAL_CANDIDATE_BATCH: "4"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.48,<6" "safetensors>=0.4" pytest

      - name: Run matched compact latent-goal experiment
        id: experiment
        continue-on-error: true
        run: |
          set -o pipefail
          {
            python -m pytest -q arcgpt2/tests
            python -m arcgpt2.build_goal_version_compact \
              --output-dir outputs/goal_smoke/data \
              --train-games 4 \
              --validation-games 1 \
              --test-games 1 \
              --seed-base 161803
            python -m arcgpt2.goal_version_runtime \
              --initialization pretrained \
              --data-dir outputs/goal_smoke/data \
              --output-dir outputs/goal_smoke/pretrained \
              --max-length 512 \
              --prefix-keep 96 \
              --batch-size 1 \
              --gradient-accumulation-steps 1 \
              --learning-rate 0.0001 \
              --max-optimizer-steps 2 \
              --warmup-steps 1 \
              --freeze-first-n-blocks 11 \
              --evaluation-games 1 \
              --no-save-model
            python -m arcgpt2.goal_version_runtime \
              --initialization random \
              --data-dir outputs/goal_smoke/data \
              --output-dir outputs/goal_smoke/random \
              --max-length 512 \
              --prefix-keep 96 \
              --batch-size 1 \
              --gradient-accumulation-steps 1 \
              --learning-rate 0.0001 \
              --max-optimizer-steps 2 \
              --warmup-steps 1 \
              --freeze-first-n-blocks 11 \
              --evaluation-games 1 \
              --no-save-model
            python -m arcgpt2.compare_goal_version \
              --pretrained outputs/goal_smoke/pretrained/summary.json \
              --random outputs/goal_smoke/random/summary.json \
              --output outputs/goal_smoke/comparison.json
          } 2>&1 | tee goal-smoke.log

      - name: Write durable evidence
        if: always()
        env:
          EXPERIMENT_OUTCOME: ${{ steps.experiment.outcome }}
        run: |
          mkdir -p reports/arcgpt2
          python - <<'PY'
          import json
          import os
          import shutil
          from datetime import datetime, timezone
          from pathlib import Path

          reports = Path("reports/arcgpt2")
          outputs = Path("outputs/goal_smoke")
          receipt = {
              "status": os.environ.get("EXPERIMENT_OUTCOME", "unknown"),
              "branch": os.environ.get("GITHUB_REF_NAME"),
              "commit_sha": os.environ.get("GITHUB_SHA"),
              "run_id": os.environ.get("GITHUB_RUN_ID"),
              "run_attempt": os.environ.get("GITHUB_RUN_ATTEMPT"),
              "recorded_at_utc": datetime.now(timezone.utc).isoformat(),
              "scope": (
                  "CPU runtime smoke for compact set-valued latent-goal scoring; "
                  "not an ARC-AGI-3 capability result"
              ),
          }
          (reports / "latest-goal-smoke-status.json").write_text(
              json.dumps(receipt, indent=2), encoding="utf-8"
          )
          copies = {
              outputs / "data" / "manifest.json": reports / "latest-goal-smoke-data-manifest.json",
              outputs / "pretrained" / "summary.json": reports / "latest-goal-smoke-pretrained.json",
              outputs / "random" / "summary.json": reports / "latest-goal-smoke-random.json",
              outputs / "comparison.json": reports / "latest-goal-smoke-comparison.json",
          }
          for source, target in copies.items():
              if source.exists():
                  shutil.copy2(source, target)
          log = Path("goal-smoke.log")
          if log.exists():
              lines = log.read_text(encoding="utf-8", errors="replace").splitlines()
              (reports / "latest-goal-smoke-log-tail.txt").write_text(
                  "\n".join(lines[-1200:]) + "\n", encoding="utf-8"
              )
          PY

      - name: Publish evidence
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/arcgpt2/
          git commit -m "Record ARC-GPT2 goal smoke ${GITHUB_RUN_ID}" || true
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arc-gpt2-goal-smoke-evidence
          path: |
            reports/arcgpt2/
            outputs/goal_smoke/data/manifest.json
            outputs/goal_smoke/pretrained/summary.json
            outputs/goal_smoke/random/summary.json
            outputs/goal_smoke/comparison.json
            goal-smoke.log
          if-no-files-found: warn
          retention-days: 14

      - name: Propagate failure
        if: steps.experiment.outcome != 'success'
        run: exit 1
