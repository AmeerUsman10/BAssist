name: ARC-GPT2 Factorized Action Semantics

on:
  push:
    branches: [arc-gpt2-programs]
    paths:
      - "arcgpt2/natural_protocol.py"
      - "arcgpt2/build_factorized_dataset.py"
      - "arcgpt2/train_factorized.py"
      - "arcgpt2/train_factorized_frozen.py"
      - "arcgpt2/compare_factorized.py"
      - ".github/workflows/arc-gpt2-factorized.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: arc-gpt2-factorized-${{ github.ref }}
  cancel-in-progress: false

jobs:
  experiment:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
          python -m pip install "transformers>=4.48,<6" "safetensors>=0.4"

      - name: Run matched pretrained and random GPT-2 experiment
        id: experiment
        continue-on-error: true
        env:
          TOKENIZERS_PARALLELISM: "false"
          HF_HUB_DISABLE_TELEMETRY: "1"
        run: |
          set -o pipefail
          {
            python -m arcgpt2.build_factorized_dataset \
              --output-dir outputs/factorized/data \
              --train-games 192 \
              --validation-games 24 \
              --test-games 32 \
              --seed-base 112358
            python -m arcgpt2.train_factorized_frozen \
              --initialization pretrained \
              --data-dir outputs/factorized/data \
              --output-dir outputs/factorized/pretrained \
              --max-length 512 \
              --prefix-keep 96 \
              --batch-size 4 \
              --gradient-accumulation-steps 2 \
              --learning-rate 0.00008 \
              --max-optimizer-steps 200 \
              --warmup-steps 16 \
              --freeze-first-n-blocks 8 \
              --evaluation-games 24 \
              --score-batch-size 16 \
              --max-plan-depth 64 \
              --no-save-model
            python -m arcgpt2.train_factorized_frozen \
              --initialization random \
              --data-dir outputs/factorized/data \
              --output-dir outputs/factorized/random \
              --max-length 512 \
              --prefix-keep 96 \
              --batch-size 4 \
              --gradient-accumulation-steps 2 \
              --learning-rate 0.00008 \
              --max-optimizer-steps 200 \
              --warmup-steps 16 \
              --freeze-first-n-blocks 8 \
              --evaluation-games 24 \
              --score-batch-size 16 \
              --max-plan-depth 64 \
              --no-save-model
            python -m arcgpt2.compare_factorized \
              --pretrained outputs/factorized/pretrained/summary.json \
              --random outputs/factorized/random/summary.json \
              --output outputs/factorized/comparison.json
          } 2>&1 | tee factorized.log

      - name: Write durable evidence
        if: always()
        env:
          EXPERIMENT_OUTCOME: ${{ steps.experiment.outcome }}
        run: |
          mkdir -p reports/arcgpt2
          python - <<'PY'
          import json
          import os
          import shutil
          from datetime import datetime, timezone
          from pathlib import Path

          reports = Path("reports/arcgpt2")
          outputs = Path("outputs/factorized")
          receipt = {
              "status": os.environ.get("EXPERIMENT_OUTCOME", "unknown"),
              "branch": os.environ.get("GITHUB_REF_NAME"),
              "commit_sha": os.environ.get("GITHUB_SHA"),
              "run_id": os.environ.get("GITHUB_RUN_ID"),
              "run_attempt": os.environ.get("GITHUB_RUN_ATTEMPT"),
              "recorded_at_utc": datetime.now(timezone.utc).isoformat(),
              "scope": (
                  "Natural-language factorized hidden-action semantics with frozen "
                  "GPT-2 embeddings and one model per matched run; not ARC-AGI-3 evaluation"
              ),
          }
          (reports / "latest-factorized-status.json").write_text(
              json.dumps(receipt, indent=2), encoding="utf-8"
          )
          copies = {
              outputs / "data" / "manifest.json": reports / "latest-factorized-data-manifest.json",
              outputs / "pretrained" / "summary.json": reports / "latest-factorized-pretrained.json",
              outputs / "random" / "summary.json": reports / "latest-factorized-random.json",
              outputs / "comparison.json": reports / "latest-factorized-comparison.json",
          }
          for source, target in copies.items():
              if source.exists():
                  shutil.copy2(source, target)
          log = Path("factorized.log")
          if log.exists():
              lines = log.read_text(encoding="utf-8", errors="replace").splitlines()
              (reports / "latest-factorized-log-tail.txt").write_text(
                  "\n".join(lines[-700:]) + "\n", encoding="utf-8"
              )
          PY

      - name: Publish evidence
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/arcgpt2/
          git commit -m "Record ARC-GPT2 factorized run ${GITHUB_RUN_ID}" || true
          git pull --rebase origin "${GITHUB_REF_NAME}"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      - name: Upload evidence artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: arc-gpt2-factorized-evidence
          path: |
            reports/arcgpt2/
            outputs/factorized/data/manifest.json
            outputs/factorized/pretrained/summary.json
            outputs/factorized/random/summary.json
            outputs/factorized/comparison.json
            factorized.log
          if-no-files-found: warn
          retention-days: 14

      - name: Propagate failure
        if: steps.experiment.outcome != 'success'
        run: exit 1
