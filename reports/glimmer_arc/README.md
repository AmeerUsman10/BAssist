# Muse Glimmer ARC gate evidence

Immutable run evidence for issue #18 is written below `reports/glimmer_arc/runs/<source-sha>/<github-run-id>/` by the bounded GitHub Actions/Kaggle workflow.

The directory must contain only sanitized JSON/text receipts and hashes. Model weights, GGUF files, credentials, raw hidden reasoning, recordings, competition artifacts, and unrestricted environment dumps are forbidden.
